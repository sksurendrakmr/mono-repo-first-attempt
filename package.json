{
  "dependencies": {
    "yarn": "^1.22.19"
  },
  "name": "v2",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "lerna": "^5.4.3"
  },
  "workspaces": {
    "packages":[
      "packages/*",
      "playground/*"
    ],
    "nohoist": [
      "packages which should not hosited in root node modules"
    ]
  },
  "private":true,
  "scripts": {
    "build":"yarn lerna run build"
  }
}
